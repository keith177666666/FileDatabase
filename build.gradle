plugins {
    id("java")
    id("maven-publish")
}

group = "dev.keith.fileDataBase"
version = current_version

repositories {
    mavenCentral()
    maven { url = "https://jitpack.io" }
}

dependencies {
    implementation("com.github.keith177666666:Database-java:$database_version")
    implementation("org.jetbrains:annotations:24.0.0")
}

tasks.test {
    useJUnitPlatform()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}