plugins {
    id("java")
    id("maven-publish")
}

group = "dev.keith"
version = "v0.1"

repositories {
    mavenCentral()
    maven { url = "https://jitpack.io" }
}

dependencies {
    implementation("com.github.keith177666666:Database-java:$database_version")
    implementation("org.jetbrains:annotations:24.0.0")
}

tasks.test {
    useJUnitPlatform()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}